# Local Claude Code / preview dev config (not needed for deployment)
.claude/

# OS junk
Thumbs.db
.DS_Store

# Stray local video recordings (never deploy; exceed GitHub's file-size limit)
*.mp4
# ...except the deployable, recompressed hero video (kept under 100 MB)
!Video.mp4
